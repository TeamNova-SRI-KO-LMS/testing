# Requirements Traceability Matrix

<!-- Generated by scripts/generate-traceability.js — do not edit by hand. -->

Each requirement in [REQUIREMENTS_CATALOGUE.md](../REQUIREMENTS_CATALOGUE.md) mapped to the test cases that verify it and the endpoints that implement it.

Generated: 2026-09-05T04:19:55.503Z

## Summary

| Metric | Value |
| --- | --- |
| Requirements catalogued | 30 |
| Requirements with at least one test | 30 |
| Requirements with no test | 0 |
| **Requirement coverage** | **100%** |
| Documented test cases | 376 |

## Matrix

| Requirement | Title | Unit | Integration | Security | E2E | Perf | Total | Endpoints | Known defects |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `FR-01` | User Registration | 7 | 6 | 0 | 7 | 0 | 20 | 2/2 | — |
| `FR-02` | Authentication | 2 | 5 | 0 | 7 | 1 | 15 | 2/2 | DEFECT-15 |
| `FR-03` | Google OAuth Authentication | 0 | 6 | 0 | 0 | 0 | 6 | 1/1 | — |
| `FR-04` | Session and Token Management | 8 | 4 | 0 | 7 | 0 | 19 | 2/2 | DEFECT-14 |
| `FR-05` | Role-Based Access Control | 4 | 7 | 0 | 12 | 0 | 23 | 25/25 | — |
| `FR-06` | User Profile Management | 0 | 6 | 0 | 0 | 0 | 6 | 5/5 | DEFECT-21, DEFECT-21, DEFECT-21 |
| `FR-07` | Password Management | 3 | 3 | 0 | 0 | 0 | 6 | 1/1 | DEFECT-20 |
| `FR-08` | Course Catalogue and Search | 0 | 6 | 0 | 7 | 1 | 14 | 2/2 | — |
| `FR-09` | Course Authoring | 2 | 4 | 0 | 0 | 0 | 6 | 3/3 | DEFECT-18 |
| `FR-10` | Course Enrolment | 4 | 6 | 0 | 5 | 0 | 15 | 3/3 | — |
| `FR-11` | Learning Progress and Completion | 3 | 4 | 0 | 5 | 0 | 12 | 2/2 | — |
| `FR-12` | Course Reviews and Ratings | 1 | 3 | 0 | 0 | 0 | 4 | 1/1 | DEFECT-19 |
| `FR-13` | Subscription Plan Management | 5 | 9 | 0 | 2 | 0 | 16 | 6/6 | — |
| `FR-14` | Payment Processing and Invoicing | 7 | 14 | 0 | 0 | 0 | 21 | 11/11 | DEFECT-22, DEFECT-08 |
| `FR-15` | Certificate Issuance and Delivery | 2 | 17 | 0 | 5 | 0 | 24 | 10/10 | DEFECT-29 |
| `FR-16` | Announcements | 0 | 10 | 0 | 0 | 0 | 10 | 10/10 | — |
| `FR-17` | Discussion Forums | 0 | 11 | 0 | 0 | 0 | 11 | 13/13 | — |
| `FR-18` | Notifications | 0 | 10 | 0 | 0 | 0 | 10 | 14/14 | DEFECT-24, DEFECT-25, DEFECT-26 |
| `FR-19` | Administrative User Management | 0 | 11 | 0 | 5 | 0 | 16 | 9/9 | — |
| `FR-20` | Administrative Course Management | 0 | 6 | 0 | 5 | 0 | 11 | 5/5 | — |
| `FR-21` | Analytics and Reporting | 0 | 10 | 0 | 5 | 0 | 15 | 7/7 | DEFECT-28 |
| `FR-22` | System Settings | 0 | 6 | 0 | 0 | 0 | 6 | 7/7 | — |
| `FR-23` | Enquiry Submission (Join Us) | 1 | 8 | 0 | 2 | 0 | 11 | 6/6 | — |
| `FR-24` | File Upload | 5 | 3 | 0 | 0 | 0 | 8 | 1/1 | — |
| `FR-25` | Health and Observability | 0 | 4 | 0 | 0 | 1 | 5 | 3/3 | — |
| `NFR-01` | Performance | 0 | 0 | 0 | 0 | 4 | 4 | — | — |
| `NFR-02` | Availability and Data Integrity | 0 | 3 | 0 | 0 | 2 | 5 | — | DEFECT-12 |
| `NFR-03` | Security | 1 | 13 | 30 | 0 | 0 | 44 | — | DEFECT-11, DEFECT-13, DEFECT-16, DEFECT-02, DEFECT-02, DEFECT-01, DEFECT-32, DEFECT-03, DEFECT-31, DEFECT-05, DEFECT-06 |
| `NFR-04` | Input Validation | 2 | 1 | 0 | 0 | 0 | 3 | — | DEFECT-23 |
| `NFR-05` | Usability and Responsiveness | 0 | 0 | 0 | 10 | 0 | 10 | — | — |

## Requirement detail

### FR-01 — User Registration

A visitor can create an account with a name, an email address and a password that meets the complexity policy. Email addresses are unique and case-insensitive; the password is stored only as a bcrypt hash.

**Implemented by:**

- `POST /api/auth/register`
- `POST /api/admin/users`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-01` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-02` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-03` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-04` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-05` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-06` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-07` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-01-01` A new student is registered and receives an access token | Integration | P1 | Pass |
| `TC-FR-01-02` The registration response never contains password material | Integration | P1 | Pass |
| `TC-FR-01-03` The password is stored only as a bcrypt hash | Integration | P1 | Pass |
| `TC-FR-01-04` Registration is rejected when the email is already in use | Integration | P1 | Pass |
| `TC-FR-01-05` Registration is rejected when the password does not meet the policy | Integration | P1 | Pass |
| `TC-FR-01-06` MongoDB rejects a second user with the same email address | Integration | P1 | Pass |
| `TC-FR-01-U01` A user built from valid attributes passes schema validation | Unit | P1 | Pass |
| `TC-FR-01-U02` A user without a name is rejected | Unit | P1 | Pass |
| `TC-FR-01-U03` A name longer than 50 characters is rejected at the boundary | Unit | P2 | Pass |
| `TC-FR-01-U04` The password is replaced by a bcrypt hash before the document is saved | Unit | P1 | Pass |
| `TC-FR-01-U05` An unchanged password is not re-hashed on save | Unit | P1 | Pass |
| `TC-FR-01-U06` A well-formed registration body raises no validation errors | Unit | P1 | Pass |
| `TC-FR-01-U07` The password policy requires an uppercase letter, a lowercase letter and a digit | Unit | P1 | Pass |

### FR-02 — Authentication

A registered user exchanges their credentials for a JSON Web Token. Deactivated accounts are refused, and failure messages do not reveal whether an account exists.

**Implemented by:**

- `POST /api/auth/login`
- `POST /api/auth/admin-login`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-01` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-02` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-03` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-04` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-05` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-06` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-07` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-02-01` A registered user logs in and receives an access token | Integration | P1 | Pass |
| `TC-FR-02-02` Login is refused for an unknown email address | Integration | P1 | Pass |
| `TC-FR-02-03` Login is refused for a wrong password | Integration | P1 | Pass |
| `TC-FR-02-04` Login is refused for a deactivated account | Integration | P1 | Pass |
| `TC-FR-02-05` Password login against a Google-only account is refused, not an error | Integration | P2 | ⚠️ DEFECT-15 |
| `TC-FR-02-U01` matchPassword accepts the correct password and rejects anything else | Unit | P1 | Pass |
| `TC-FR-02-U02` getSignedJwtToken issues a token carrying the user id and an expiry | Unit | P1 | Pass |
| `TC-PERF-LOAD` load scenario | Performance | P1 | Not run in this report |

### FR-03 — Google OAuth Authentication

A user can sign in with a Google account. The ID token is verified against Google before any account is created or linked.

**Implemented by:**

- `POST /api/auth/google`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-03-01` Google sign-in is refused when no credential is supplied | Integration | P1 | Pass |
| `TC-FR-03-02` Google sign-in is refused for a credential Google does not verify | Integration | P1 | Pass |
| `TC-FR-03-03` A first-time Google user is registered with the role they choose | Integration | P1 | Pass |
| `TC-FR-03-04` A first-time Google user must choose a role before an account is created | Integration | P1 | Pass |
| `TC-FR-03-05` Signing in with Google links the provider to an existing local account | Integration | P1 | Pass |
| `TC-FR-03-06` Google sign-in is refused for a deactivated account | Integration | P1 | Pass |

### FR-04 — Session and Token Management

Every protected endpoint requires a valid, unexpired token belonging to an active account. The current user can read their own identity and sign out.

**Implemented by:**

- `GET /api/auth/me`
- `POST /api/auth/logout`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-01` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-02` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-03` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-04` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-05` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-06` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-07` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-04-01` An authenticated user retrieves their own profile | Integration | P1 | Pass |
| `TC-FR-04-02` An unauthenticated request for the profile is refused | Integration | P1 | Pass |
| `TC-FR-04-03` An authenticated user logs out | Integration | P2 | Pass |
| `TC-FR-04-04` A token stops working after the user logs out | Integration | P2 | ⚠️ DEFECT-14 |
| `TC-FR-04-U01` protect attaches the user and continues given a valid token | Unit | P1 | Pass |
| `TC-FR-04-U02` protect rejects a request that carries no Authorization header | Unit | P1 | Pass |
| `TC-FR-04-U03` protect rejects an Authorization header that is not a Bearer scheme | Unit | P2 | Pass |
| `TC-FR-04-U04` protect rejects a token signed with the wrong secret | Unit | P1 | Pass |
| `TC-FR-04-U05` protect rejects an expired token | Unit | P1 | Pass |
| `TC-FR-04-U06` protect rejects a token whose subject no longer exists | Unit | P1 | Pass |
| `TC-FR-04-U07` protect rejects a valid token belonging to a deactivated account | Unit | P1 | Pass |
| `TC-FR-04-U08` protect fails closed when the user lookup throws | Unit | P1 | Pass |

### FR-05 — Role-Based Access Control

Every endpoint enforces the roles it declares. Students, instructors and administrators see only what their role permits, and an instructor may act only on their own courses.

**Implemented by:**

- `GET /api/admin/*`
- `PUT /api/admin/*`
- `POST /api/admin/*`
- `DELETE /api/admin/*`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-01` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-02` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-03` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-04` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-05` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-06` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-07` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-13` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-14` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-15` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-16` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-17` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-05-01` An administrator signs in through the admin login endpoint | Integration | P1 | Pass |
| `TC-FR-05-02` A non-administrator is refused at the admin login endpoint | Integration | P1 | Pass |
| `TC-FR-05-03` A student cannot create a course | Integration | P1 | Pass |
| `TC-FR-05-04` An instructor cannot update a course belonging to someone else | Integration | P1 | Pass |
| `TC-FR-05-05` A student cannot list the user directory | Integration | P1 | Pass |
| `TC-FR-05-06` A student cannot read the administrative statistics | Integration | P1 | Pass |
| `TC-FR-05-07` A student cannot read the system settings | Integration | P1 | Pass |
| `TC-FR-05-U01` authorize admits a user whose role is in the allow-list | Unit | P1 | Pass |
| `TC-FR-05-U02` authorize refuses a user whose role is outside the allow-list | Unit | P1 | Pass |
| `TC-FR-05-U03` checkCourseAccess grants an admin access to any course without an enrolment check | Unit | P1 | Pass |
| `TC-FR-05-U04` checkCourseAccess grants the owning instructor access to their own course | Unit | P1 | Pass |

### FR-06 — User Profile Management

A user maintains their own profile, notification preferences and privacy settings. A profile update can never change the account’s role or email address.

**Implemented by:**

- `GET /api/users/profile`
- `PUT /api/users/profile`
- `PUT /api/users/notifications`
- `PUT /api/users/privacy`
- `PUT /api/users/last-login`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-06-01` A user retrieves their own profile | Integration | P1 | Pass |
| `TC-FR-06-02` A user updates their own profile fields | Integration | P1 | Pass |
| `TC-FR-06-03` A profile update cannot change the caller’s role or email | Integration | P1 | Pass |
| `TC-FR-06-04` A user updates their notification preferences | Integration | P1 | ⚠️ DEFECT-21 |
| `TC-FR-06-05` A user updates their privacy settings | Integration | P1 | ⚠️ DEFECT-21 |
| `TC-FR-06-06` A user records their most recent login time | Integration | P2 | ⚠️ DEFECT-21 |

### FR-07 — Password Management

A signed-in user changes their password by supplying the current one. Reset tokens are single-use, expire after ten minutes and are stored only as a hash.

**Implemented by:**

- `PUT /api/users/password`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-07-01` A user changes their password and can log in with the new one | Integration | P1 | Pass |
| `TC-FR-07-02` A password change is refused when the current password is wrong | Integration | P1 | Pass |
| `TC-FR-07-03` A password change enforces the same policy as registration | Integration | P2 | ⚠️ DEFECT-20 |
| `TC-FR-07-U01` A password shorter than 6 characters is rejected at the boundary | Unit | P1 | Pass |
| `TC-FR-07-U02` getResetPasswordToken returns a raw token but stores only its SHA-256 hash | Unit | P1 | Pass |
| `TC-FR-07-U03` A password reset token expires ten minutes after it is issued | Unit | P2 | Pass |

### FR-08 — Course Catalogue and Search

Anyone can browse, filter, search and paginate the published course catalogue and open a course detail page, without signing in.

**Implemented by:**

- `GET /api/courses`
- `GET /api/courses/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-08` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-09` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-10` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-11` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-12` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-18` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-19` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-08-01` The course catalogue is readable without authentication | Integration | P1 | Pass |
| `TC-FR-08-02` The catalogue paginates with page and limit | Integration | P2 | Pass |
| `TC-FR-08-03` The catalogue filters by category, level and published state | Integration | P2 | Pass |
| `TC-FR-08-04` The catalogue search matches course titles case-insensitively | Integration | P2 | Pass |
| `TC-FR-08-05` A single course is readable without authentication | Integration | P1 | Pass |
| `TC-FR-08-06` Requesting a course that does not exist returns 404 | Integration | P2 | Pass |
| `TC-PERF-LOAD` load scenario | Performance | P1 | Not run in this report |

### FR-09 — Course Authoring

An instructor creates, updates and deletes their own courses, including the weekly curriculum and its lessons. An administrator may act on any course.

**Implemented by:**

- `POST /api/courses`
- `PUT /api/courses/:id`
- `DELETE /api/courses/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-09-01` An instructor creates a course and is recorded as its owner | Integration | P1 | Pass |
| `TC-FR-09-02` The owning instructor updates their own course | Integration | P1 | Pass |
| `TC-FR-09-03` The owning instructor deletes their own course | Integration | P1 | Pass |
| `TC-FR-09-04` Deleting a course also removes the enrolment records that point at it | Integration | P2 | ⚠️ DEFECT-18 |
| `TC-FR-09-U01` A well-formed course payload raises no validation errors | Unit | P1 | Pass |
| `TC-FR-09-U02` A course built from valid attributes passes schema validation | Unit | P1 | Pass |

### FR-10 — Course Enrolment

A student enrols in a published course and may un-enrol. Enrolment creates a progress record and is reflected on both the course roster and the student’s account. A student cannot enrol twice.

**Implemented by:**

- `POST /api/courses/:id/enroll`
- `DELETE /api/courses/:id/enroll`
- `GET /api/courses/my-courses`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-08` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-09` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-10` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-11` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-12` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-10-01` A student sees only the courses they are enrolled in | Integration | P1 | Pass |
| `TC-FR-10-02` A student enrols in a published course | Integration | P1 | Pass |
| `TC-FR-10-03` A student cannot enrol twice in the same course | Integration | P1 | Pass |
| `TC-FR-10-04` A student cannot enrol in an unpublished course | Integration | P1 | Pass |
| `TC-FR-10-05` A student un-enrols and every trace of the enrolment is removed | Integration | P1 | Pass |
| `TC-FR-10-06` MongoDB rejects a duplicate enrolment for the same student and course | Integration | P1 | Pass |
| `TC-FR-10-U01` checkCourseAccess returns 404 for a course that does not exist | Unit | P2 | Pass |
| `TC-FR-10-U02` checkCourseAccess grants an enrolled student access and attaches their progress | Unit | P1 | Pass |
| `TC-FR-10-U03` checkCourseAccess refuses a student who is not enrolled | Unit | P1 | Pass |
| `TC-FR-10-U04` A unique compound index prevents a student enrolling twice in one course | Unit | P1 | Pass |

### FR-11 — Learning Progress and Completion

Progress is tracked per enrolment as a percentage of lessons completed. Marking a course complete stamps a completion date, which drives certificate eligibility and the analytics dashboard.

**Implemented by:**

- `POST /api/courses/:id/complete`
- `GET /api/users/dashboard`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-08` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-09` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-10` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-11` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-12` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-11-01` An enrolled student marks a course complete | Integration | P1 | Pass |
| `TC-FR-11-02` A course cannot be completed twice | Integration | P2 | Pass |
| `TC-FR-11-03` A student who is not enrolled cannot mark the course complete | Integration | P1 | Pass |
| `TC-FR-11-04` The dashboard reports the caller’s enrolments and progress | Integration | P1 | Pass |
| `TC-FR-11-U01` Marking a course complete stamps the completion date automatically | Unit | P1 | Pass |
| `TC-FR-11-U02` Reverting a completion clears the completion date | Unit | P2 | Pass |
| `TC-FR-11-U03` calculateProgress returns the percentage of lessons completed | Unit | P1 | Pass |

### FR-12 — Course Reviews and Ratings

A user reviews a course once, with a rating of one to five and an optional comment. The course average is recalculated on every change.

**Implemented by:**

- `POST /api/courses/:id/reviews`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-12-01` A student reviews a course and the average rating is recalculated | Integration | P1 | Pass |
| `TC-FR-12-02` A user cannot review the same course twice | Integration | P1 | Pass |
| `TC-FR-12-03` Only an enrolled student may review a course | Integration | P2 | ⚠️ DEFECT-19 |
| `TC-FR-12-U01` The average rating is the mean of every review rating | Unit | P1 | Pass |

### FR-13 — Subscription Plan Management

Three plans — starter, pro and premium — with monthly and yearly billing. A user holds one active subscription, may upgrade but not downgrade through the upgrade endpoint, and may cancel. Usage is reported against the plan limits.

**Implemented by:**

- `GET /api/subscriptions/plans`
- `GET /api/subscriptions/current`
- `POST /api/subscriptions/create`
- `PUT /api/subscriptions/upgrade`
- `PUT /api/subscriptions/cancel`
- `GET /api/subscriptions/usage`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-18` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-19` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-13-01` The pricing page reads the three plans without authentication | Integration | P1 | Pass |
| `TC-FR-13-02` A user starts a 14-day trial on the pro plan | Integration | P1 | Pass |
| `TC-FR-13-03` The free starter plan activates immediately with no payment record | Integration | P1 | Pass |
| `TC-FR-13-04` A user cannot hold two active subscriptions at once | Integration | P1 | Pass |
| `TC-FR-13-05` A subscriber reads their own current subscription | Integration | P1 | Pass |
| `TC-FR-13-06` A pro subscriber upgrades to premium | Integration | P1 | Pass |
| `TC-FR-13-07` A downgrade is refused through the upgrade endpoint | Integration | P1 | Pass |
| `TC-FR-13-08` A subscriber cancels and auto-renewal is switched off | Integration | P1 | Pass |
| `TC-FR-13-09` Usage is reported against the plan limits | Integration | P2 | Pass |
| `TC-FR-13-U01` A starter subscription may create courses up to its limit and no further | Unit | P1 | Pass |
| `TC-FR-13-U02` Student enrolment is allowed only while the batch fits inside the plan limit | Unit | P1 | Pass |
| `TC-FR-13-U03` Cancelling records the reason and stops auto-renewal | Unit | P1 | Pass |
| `TC-FR-13-U04` Renewing a monthly subscription extends it by 30 days from now | Unit | P1 | Pass |
| `TC-FR-13-U05` Each plan exposes its documented feature set | Unit | P2 | Pass |

### FR-14 — Payment Processing and Invoicing

Payments are recorded against a subscription, completed or failed with a gateway reference, and refundable once completed. Every payment receives a unique invoice number, and a customer can read only their own payments and invoices.

**Implemented by:**

- `POST /api/payments/create`
- `PUT /api/payments/:id/complete`
- `PUT /api/payments/:id/fail`
- `POST /api/payments/:id/refund`
- `GET /api/payments/*`
- `GET /api/subscriptions/payments`
- `GET /api/subscriptions/invoice/:id`
- `PUT /api/admin/payments/:id/status`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-14-01` A subscriber reads only their own payment history | Integration | P1 | Pass |
| `TC-FR-14-02` A subscriber downloads their own invoice | Integration | P1 | Pass |
| `TC-FR-14-03` The administrator reads payment statistics | Integration | P1 | Pass |
| `TC-FR-14-04` An administrator marks a pending payment as completed | Integration | P1 | Pass |
| `TC-FR-14-05` A subscriber records a payment against their own subscription | Integration | P1 | Pass |
| `TC-FR-14-06` Completing a payment records the gateway reference and stamps it paid | Integration | P1 | Pass |
| `TC-FR-14-07` A payment cannot be completed twice | Integration | P1 | Pass |
| `TC-FR-14-08` A failed payment records the reason and stays unpaid | Integration | P1 | Pass |
| `TC-FR-14-09` A completed payment is refunded in part | Integration | P1 | Pass |
| `TC-FR-14-10` Only a completed payment can be refunded | Integration | P1 | Pass |
| `TC-FR-14-11` A refund cannot exceed the amount that was paid | Integration | P1 | ⚠️ DEFECT-22 |
| `TC-FR-14-12` A user reads their own payment | Integration | P2 | Pass |
| `TC-FR-14-13` An administrator reads aggregate payment statistics | Integration | P1 | Pass |
| `TC-FR-14-14` MongoDB rejects a duplicate invoice number | Integration | P1 | Pass |
| `TC-FR-14-U01` Invoice and receipt numbers are protected by sparse unique indexes | Unit | P1 | Pass |
| `TC-FR-14-U02` Completing a payment records the gateway reference and both timestamps | Unit | P1 | Pass |
| `TC-FR-14-U03` Failing a payment records the reason and leaves it unpaid | Unit | P1 | Pass |
| `TC-FR-14-U04` A partial refund records only the amount refunded | Unit | P1 | Pass |
| `TC-FR-14-U05` A generated invoice number follows the INV-YYYYMM-NNNN format | Unit | P2 | Pass |
| `TC-FR-14-U06` A new payment is assigned an invoice number automatically | Unit | P1 | Pass |
| `TC-FR-14-U07` Two invoice numbers issued in the same month differ even when the RNG repeats | Unit | P1 | ⚠️ DEFECT-08 |

### FR-15 — Certificate Issuance and Delivery

An administrator issues a certificate to a student who has completed a course. Certificate numbers are unique and sequential per year. A student sees only their own certificates and the first viewing is recorded.

**Implemented by:**

- `GET /api/certificates*`
- `POST /api/certificates`
- `POST /api/certificates/:id/send`
- `POST /api/certificates/:id/mark-viewed`
- `PUT /api/certificates/:id/status`
- `DELETE /api/certificates/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-08` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-09` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-10` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-11` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-12` 02-enrolment.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-15-01` A student who completed a course appears as eligible for a certificate | Integration | P1 | Pass |
| `TC-FR-15-02` A student who already holds a certificate is no longer eligible | Integration | P1 | Pass |
| `TC-FR-15-03` An administrator issues a certificate to a graduate | Integration | P1 | Pass |
| `TC-FR-15-04` A certificate cannot be issued for a course the student has not completed | Integration | P1 | Pass |
| `TC-FR-15-05` An administrator lists every issued certificate | Integration | P1 | Pass |
| `TC-FR-15-06` Certificate statistics break down by status | Integration | P2 | Pass |
| `TC-FR-15-07` A student sees only their own certificates | Integration | P1 | Pass |
| `TC-FR-15-08` A student marks their certificate as viewed, and the first view is timestamped | Integration | P2 | Pass |
| `TC-FR-15-09` An administrator advances a certificate through its status workflow | Integration | P2 | Pass |
| `TC-FR-15-10` An administrator sends a certificate to the student | Integration | P2 | Pass |
| `TC-FR-15-11` An administrator revokes a certificate | Integration | P2 | Pass |
| `TC-FR-15-12` Certificate statistics count each status and group by course | Integration | P2 | Pass |
| `TC-FR-15-13` The certificate register paginates and filters by status | Integration | P2 | Pass |
| `TC-FR-15-14` MongoDB rejects a duplicate certificate number | Integration | P1 | Pass |
| `TC-FR-15-15` Certificates issued one after another receive consecutive numbers | Integration | P1 | Pass |
| `TC-FR-15-16` Certificates issued concurrently still receive distinct numbers | Integration | P1 | ⚠️ DEFECT-29 |
| `TC-FR-15-17` The certificate register filters by status and by course | Integration | P2 | Pass |
| `TC-FR-15-U01` The first certificate of a year is numbered CERT-000001-YYYY | Unit | P1 | Pass |
| `TC-FR-15-U02` Each certificate number increments the previous highest for the year | Unit | P1 | Pass |

### FR-16 — Announcements

An administrator publishes announcements targeted at an audience and a date window. Users see only announcements addressed to them, and can mark one as read.

**Implemented by:**

- `GET /api/announcements*`
- `POST /api/announcements*`
- `PUT /api/announcements/*`
- `DELETE /api/announcements/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-16-01` A student sees announcements targeted at everyone | Integration | P1 | Pass |
| `TC-FR-16-02` A student does not see announcements aimed at instructors | Integration | P1 | Pass |
| `TC-FR-16-03` An administrator lists every announcement including expired ones | Integration | P1 | Pass |
| `TC-FR-16-04` An administrator publishes an announcement | Integration | P1 | Pass |
| `TC-FR-16-05` An administrator edits an announcement | Integration | P2 | Pass |
| `TC-FR-16-06` An administrator pins and unpins an announcement | Integration | P2 | Pass |
| `TC-FR-16-07` An administrator withdraws an announcement, removing it from the student feed | Integration | P1 | Pass |
| `TC-FR-16-08` A student marks an announcement as read | Integration | P2 | Pass |
| `TC-FR-16-09` An administrator deletes an announcement | Integration | P2 | Pass |
| `TC-FR-16-10` Active announcements are filtered by audience and by date window | Integration | P1 | Pass |

### FR-17 — Discussion Forums

An administrator manages forums by category and level. Users read forums, subscribe to them and post. A locked or inactive forum accepts no new posts.

**Implemented by:**

- `GET /api/forums*`
- `POST /api/forums*`
- `PUT /api/forums/*`
- `DELETE /api/forums/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-17-01` An authenticated user lists the active forums | Integration | P1 | Pass |
| `TC-FR-17-02` A user opens a single forum | Integration | P2 | Pass |
| `TC-FR-17-03` An administrator creates a forum | Integration | P1 | Pass |
| `TC-FR-17-04` An administrator edits a forum | Integration | P2 | Pass |
| `TC-FR-17-05` A user subscribes to a forum and then unsubscribes | Integration | P2 | Pass |
| `TC-FR-17-06` A user posts in a forum and the post count is maintained | Integration | P1 | Pass |
| `TC-FR-17-07` A locked forum accepts no new posts | Integration | P1 | Pass |
| `TC-FR-17-08` A forum’s posts are listed with pagination | Integration | P2 | Pass |
| `TC-FR-17-09` Posts are listed for one forum with pagination metadata | Integration | P2 | Pass |
| `TC-FR-17-10` A like and a dislike are mutually exclusive for one user | Integration | P2 | Pass |
| `TC-FR-17-11` Creating a post updates the forum’s counter and last-post pointer | Integration | P2 | Pass |

### FR-18 — Notifications

An administrator sends notifications to an audience, to named users, or to the parents of named students. Users see notifications addressed to them within their scheduling window and can mark them read or clicked.

**Implemented by:**

- `GET /api/notifications*`
- `POST /api/notifications*`
- `PUT /api/notifications/*`
- `DELETE /api/notifications/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-18-01` A student receives notifications addressed to everyone | Integration | P1 | Pass |
| `TC-FR-18-02` An administrator lists the users a notification can target | Integration | P2 | ⚠️ DEFECT-24 |
| `TC-FR-18-03` An administrator creates a notification | Integration | P1 | Pass |
| `TC-FR-18-04` An administrator edits a notification | Integration | P2 | Pass |
| `TC-FR-18-05` A user marks a notification as read | Integration | P2 | Pass |
| `TC-FR-18-06` An administrator sends a notification to selected users | Integration | P2 | Pass |
| `TC-FR-18-07` An administrator sends a notification to the parents of selected students | Integration | P2 | Pass |
| `TC-FR-18-08` send-to-users records the sending administrator without being told who they are | Integration | P2 | ⚠️ DEFECT-25 |
| `TC-FR-18-09` A parent notification actually reaches a parent | Integration | P2 | ⚠️ DEFECT-26 |
| `TC-FR-18-10` Active notifications respect the scheduling window and the audience | Integration | P1 | Pass |

### FR-19 — Administrative User Management

An administrator lists, creates, edits, suspends and deletes user accounts. A suspended account can no longer sign in.

**Implemented by:**

- `GET /api/users`
- `GET /api/users/:id`
- `PUT /api/users/:id`
- `DELETE /api/users/:id`
- `GET /api/admin/users`
- `POST /api/admin/users`
- `PUT /api/admin/users/:id`
- `PUT /api/admin/users/:id/status`
- `DELETE /api/admin/users/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-13` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-14` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-15` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-16` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-17` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-19-01` An administrator lists every user without any password material | Integration | P1 | Pass |
| `TC-FR-19-02` An administrator reads a single user record | Integration | P2 | Pass |
| `TC-FR-19-03` An administrator changes a user’s role | Integration | P1 | Pass |
| `TC-FR-19-04` An administrator deactivates a user, and that user can no longer log in | Integration | P1 | Pass |
| `TC-FR-19-05` An administrator deletes a user | Integration | P1 | Pass |
| `TC-FR-19-06` The administrator lists users with pagination | Integration | P1 | Pass |
| `TC-FR-19-07` An administrator provisions an instructor account | Integration | P1 | Pass |
| `TC-FR-19-08` An administrator edits a user record | Integration | P1 | Pass |
| `TC-FR-19-09` An administrator suspends and restores an account | Integration | P1 | Pass |
| `TC-FR-19-10` An administrator deletes a user account | Integration | P1 | Pass |
| `TC-FR-19-11` The user directory filters by role | Integration | P2 | Pass |

### FR-20 — Administrative Course Management

An administrator lists every course, creates one on behalf of an instructor, edits any course, and publishes or withdraws it from the public catalogue.

**Implemented by:**

- `GET /api/admin/courses`
- `POST /api/admin/courses`
- `PUT /api/admin/courses/:id`
- `PUT /api/admin/courses/:id/status`
- `DELETE /api/admin/courses/:id`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-13` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-14` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-15` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-16` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-17` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-20-01` An administrator lists every course, published or not | Integration | P1 | Pass |
| `TC-FR-20-02` An administrator creates a course on behalf of an instructor | Integration | P1 | Pass |
| `TC-FR-20-03` An administrator edits any course | Integration | P1 | Pass |
| `TC-FR-20-04` An administrator publishes and unpublishes a course | Integration | P1 | Pass |
| `TC-FR-20-05` An administrator deletes a course | Integration | P1 | Pass |
| `TC-FR-20-06` The administrative course list filters by category and status | Integration | P2 | Pass |

### FR-21 — Analytics and Reporting

The administrative dashboard reports user, course and revenue totals, a rolling analytics period, and a recent-activity feed. Only completed payments count as revenue.

**Implemented by:**

- `GET /api/admin/stats`
- `GET /api/admin/analytics`
- `GET /api/admin/analytics/export`
- `GET /api/admin/activities`
- `GET /api/admin/payment-stats`
- `GET /api/admin/recent-payments`
- `GET /api/admin/all-payments`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-13` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-14` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-15` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-16` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-17` 03-admin-course-lifecycle.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-21-01` The dashboard reports user, course and revenue totals | Integration | P1 | Pass |
| `TC-FR-21-02` The analytics endpoint reports over the requested period | Integration | P1 | Pass |
| `TC-FR-21-03` The analytics export endpoint responds to an administrator | Integration | P3 | Pass |
| `TC-FR-21-04` The activity feed merges recent records newest first | Integration | P2 | Pass |
| `TC-FR-21-05` Payment statistics separate completed revenue from other statuses | Integration | P1 | Pass |
| `TC-FR-21-06` Payment statistics honour a date range | Integration | P1 | Pass |
| `TC-FR-21-07` Revenue is broken down by plan, highest first | Integration | P1 | Pass |
| `TC-FR-21-08` Monthly revenue groups completed payments by calendar month | Integration | P1 | Pass |
| `TC-FR-21-09` A December payment is included in its own year | Integration | P2 | ⚠️ DEFECT-28 |
| `TC-FR-21-10` Revenue by plan honours a date range | Integration | P1 | Pass |

### FR-22 — System Settings

A single settings document holds site configuration. An administrator reads and updates it whole or by section, resets it to defaults, and exports or imports it.

**Implemented by:**

- `GET /api/admin/settings*`
- `PUT /api/admin/settings*`
- `POST /api/admin/settings/*`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-22-01` The first read creates and returns the default settings document | Integration | P1 | Pass |
| `TC-FR-22-02` An administrator updates the settings and the change is attributed to them | Integration | P1 | Pass |
| `TC-FR-22-03` A single settings section can be read on its own | Integration | P2 | Pass |
| `TC-FR-22-04` A single settings section can be updated on its own | Integration | P2 | Pass |
| `TC-FR-22-05` Resetting restores the default settings and leaves one document | Integration | P2 | Pass |
| `TC-FR-22-06` Settings can be exported and re-imported | Integration | P2 | Pass |

### FR-23 — Enquiry Submission (Join Us)

A prospective student submits an enquiry without an account. Administrators triage the queue through a status workflow of pending, contacted, enrolled and rejected.

**Implemented by:**

- `POST /api/join-us/submit`
- `GET /api/join-us/*`
- `PUT /api/join-us/*`
- `DELETE /api/join-us/*`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-18` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-19` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-FR-23-01` A prospective student submits an enquiry without an account | Integration | P1 | Pass |
| `TC-FR-23-02` A duplicate enquiry from the same email address is refused | Integration | P2 | Pass |
| `TC-FR-23-03` The submission records the enquirer’s IP address and user agent | Integration | P3 | Pass |
| `TC-FR-23-04` An administrator reviews the enquiry queue | Integration | P1 | Pass |
| `TC-FR-23-05` An administrator marks an enquiry as contacted | Integration | P1 | Pass |
| `TC-FR-23-06` Enquiry statistics break down by status | Integration | P2 | Pass |
| `TC-FR-23-07` An administrator deletes an enquiry | Integration | P2 | Pass |
| `TC-FR-23-08` The enquiry queue filters by status | Integration | P2 | Pass |
| `TC-FR-23-U01` A complete Join Us submission raises no validation errors | Unit | P2 | Pass |

### FR-24 — File Upload

Users upload an avatar image and administrators upload certificate documents. Only permitted file types are accepted, sizes are capped, and stored filenames are generated rather than taken from the client.

**Implemented by:**

- `POST /api/users/avatar`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-24-01` An avatar upload with no file attached is rejected | Integration | P2 | Pass |
| `TC-FR-24-02` A PNG avatar is accepted and linked to the user | Integration | P1 | Pass |
| `TC-FR-24-03` A non-image avatar upload is rejected | Integration | P1 | Pass |
| `TC-FR-24-U01` The avatar filter accepts images and rejects everything else | Unit | P1 | Pass |
| `TC-FR-24-U02` The certificate filter accepts images and PDFs only | Unit | P2 | Pass |
| `TC-FR-24-U03` Uploaded files are stored under a generated name, never the client-supplied one | Unit | P1 | Pass |
| `TC-FR-24-U04` An oversized upload is reported as HTTP 400 with a size message | Unit | P1 | Pass |
| `TC-FR-24-U05` Avatar uploads are capped at 5 MB and certificates at 10 MB | Unit | P2 | Pass |

### FR-25 — Health and Observability

The service exposes unauthenticated health endpoints reporting its own and the database’s status, answers unknown routes with a JSON 404, and works behind the Choreo deployment prefix.

**Implemented by:**

- `GET /health`
- `GET /api/health`
- `GET /api/test`

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-FR-25-01` The platform health probe reports OK without authentication | Integration | P1 | Pass |
| `TC-FR-25-02` The API health endpoint reports the database and feature status | Integration | P1 | Pass |
| `TC-FR-25-03` The routing smoke endpoint confirms the API is mounted | Integration | P3 | Pass |
| `TC-FR-25-04` An unknown route returns a JSON 404 rather than an HTML error page | Integration | P2 | Pass |
| `TC-PERF-SMOKE` smoke scenario | Performance | P1 | Not run in this report |

### NFR-01 — Performance

95 % of requests complete within 500 ms and 99 % within 1200 ms under the expected load of 50 concurrent users; fewer than 1 % of requests fail. Verified by the k6 suite in tests/performance.

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-PERF-LOAD` load scenario | Performance | P1 | Not run in this report |
| `TC-PERF-SMOKE` smoke scenario | Performance | P1 | Not run in this report |
| `TC-PERF-SPIKE` spike scenario | Performance | P1 | Not run in this report |
| `TC-PERF-STRESS` stress scenario | Performance | P1 | Not run in this report |

### NFR-02 — Availability and Data Integrity

The service recovers from a lost database connection, and database constraints — unique email, unique enrolment, unique invoice and certificate numbers — hold even when application-level checks race.

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-NFR-02-01` The Choreo-prefixed health probe answers like the direct one | Integration | P1 | Pass |
| `TC-NFR-02-02` The admin router is reachable through the Choreo prefix with the same guards | Integration | P1 | Pass |
| `TC-NFR-02-03` No schema declares an index with an empty key specification | Integration | P2 | ⚠️ DEFECT-12 |
| `TC-PERF-SPIKE` spike scenario | Performance | P1 | Not run in this report |
| `TC-PERF-STRESS` stress scenario | Performance | P1 | Not run in this report |

### NFR-03 — Security

The system addresses the OWASP Top 10 as set out in SENG 34213 §8.1: access control on every endpoint, hashed credentials, validated input, security headers, dependency scanning, and audit logging free of secrets.

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-NFR-03-01` Self-registration permits an unauthenticated caller to claim the admin role | Integration | P1 | ⚠️ DEFECT-11 |
| `TC-NFR-03-02` A validation error does not echo the submitted password back to the client | Integration | P2 | ⚠️ DEFECT-13 |
| `TC-NFR-03-03` The failure message does not reveal whether the account exists | Integration | P2 | Pass |
| `TC-NFR-03-04` A regular expression in the search term cannot enumerate the catalogue | Integration | P2 | ⚠️ DEFECT-16 |
| `TC-NFR-03-05` A student cannot read another user’s record | Integration | P1 | Pass |
| `TC-NFR-03-06` A subscriber cannot download another customer’s invoice | Integration | P1 | Pass |
| `TC-NFR-03-07` A user cannot create a payment against someone else’s subscription | Integration | P1 | Pass |
| `TC-NFR-03-08` A student cannot mark another student’s certificate as viewed | Integration | P1 | Pass |
| `TC-NFR-03-09` A student cannot read an announcement addressed to another audience | Integration | P2 | Pass |
| `TC-NFR-03-10` A student cannot read the enquiry queue | Integration | P1 | Pass |
| `TC-NFR-03-11` The health probe does not disclose the database connection string | Integration | P1 | Pass |
| `TC-NFR-03-12` Every response carries the baseline security headers | Integration | P1 | Pass |
| `TC-NFR-03-13` Google sign-up cannot be used to create an administrator | Integration | P1 | Pass |
| `TC-NFR-03-U01` The bcrypt cost factor meets the OWASP A02 minimum of 12 | Unit | P1 | ⚠️ DEFECT-02 |
| `TC-SEC-A01-01` No administrative endpoint responds to an unauthenticated request | Security | P1 | Pass |
| `TC-SEC-A01-02` No administrative endpoint responds to a student token | Security | P1 | Pass |
| `TC-SEC-A01-03` No endpoint requiring authentication responds without a token | Security | P1 | Pass |
| `TC-SEC-A01-04` A token with a tampered payload is rejected | Security | P1 | Pass |
| `TC-SEC-A01-05` A token using the "none" algorithm is rejected | Security | P1 | Pass |
| `TC-SEC-A01-06` A student cannot reach another student’s records by guessing an id | Security | P1 | Pass |
| `TC-SEC-A02-01` Passwords are stored as bcrypt hashes, never in clear text | Security | P1 | Pass |
| `TC-SEC-A02-02` The bcrypt cost factor is at least 12 | Security | P1 | ⚠️ DEFECT-02 |
| `TC-SEC-A02-03` Password reset tokens are stored hashed, never in their usable form | Security | P1 | Pass |
| `TC-SEC-A02-04` No API response exposes password material | Security | P1 | Pass |
| `TC-SEC-A03-01` An operator object in the login email does not authenticate anyone | Security | P1 | Pass |
| `TC-SEC-A03-02` An operator object in a path parameter does not match any document | Security | P1 | Pass |
| `TC-SEC-A03-03` Registration cannot inject fields the schema does not declare | Security | P1 | Pass |
| `TC-SEC-A03-04` A script payload in a profile field is stored and returned as plain text | Security | P2 | Pass |
| `TC-SEC-A03-05` A query string containing SQL keywords is rejected | Security | P2 | Pass |
| `TC-SEC-A03-06` A legitimate search term is not rejected as an attack | Security | P1 | ⚠️ DEFECT-01 |
| `TC-SEC-A04-01` A rate limiter is configured over the whole API surface | Security | P1 | Pass |
| `TC-SEC-A04-02` The limiter actually rejects a burst that exceeds the configured maximum | Security | P1 | Pass |
| `TC-SEC-A04-03` The functional suites run with the limiter neutralised, by design | Security | P2 | Pass |
| `TC-SEC-A04-04` Authentication endpoints carry a tighter limit than the rest of the API | Security | P2 | ⚠️ DEFECT-32 |
| `TC-SEC-A05-01` Error responses carry no stack trace or internal detail | Security | P1 | Pass |
| `TC-SEC-A05-02` The application sends the baseline security headers on every response | Security | P1 | Pass |
| `TC-SEC-A05-03` No credential is committed to the application repository | Security | P1 | Pass |
| `TC-SEC-A05-04` The application refuses to start without an explicit JWT secret | Security | P1 | ⚠️ DEFECT-03 |
| `TC-SEC-A07-01` The password policy rejects the most common weak passwords | Security | P1 | Pass |
| `TC-SEC-A07-02` An account is locked after repeated failed login attempts | Security | P1 | ⚠️ DEFECT-31 |
| `TC-SEC-A07-03` The session cookie is HTTP-only, so script cannot read it | Security | P2 | Pass |
| `TC-SEC-A09-01` No application log statement prints a secret | Security | P1 | ⚠️ DEFECT-05 |
| `TC-SEC-A09-02` Authentication outcomes are logged for audit | Security | P2 | Pass |
| `TC-SEC-A10-01` A user-supplied website URL is validated against an allow-list | Security | P2 | ⚠️ DEFECT-06 |

### NFR-04 — Input Validation

Every write endpoint validates its input before it reaches the database, and reports every offending field in a single response rather than one at a time.

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-NFR-04-01` A refund request without a reason is rejected | Integration | P2 | ⚠️ DEFECT-23 |
| `TC-NFR-04-U01` handleValidationErrors continues when no rule failed | Unit | P1 | Pass |
| `TC-NFR-04-U02` handleValidationErrors returns 400 with the collected errors | Unit | P1 | Pass |

### NFR-05 — Usability and Responsiveness

The interface is usable on a phone-sized viewport: no page scrolls horizontally, and primary actions stay within the viewport. Verified by the Playwright suite.

**Verified by:**

| Test case | Layer | Priority | Status |
| --- | --- | --- | --- |
| `TC-E2E-01` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-02` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-03` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-04` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-05` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-06` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-07` 01-authentication.critical — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-18` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-19` 04-public-pages — browser flow | E2E | P1 | Not run in this report |
| `TC-E2E-20` 05-responsive — browser flow | E2E | P1 | Not run in this report |

